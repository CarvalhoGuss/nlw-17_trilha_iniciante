// arrays, objetos
let meta = {
    value: "ler um livro todo por mês", 
    checked: false,
    isChecked: (info)
}

console.log(meta.value)
meta.isChecked()

// function // arrow function
const criarMeta = () => {}

function criarMeta() {}